import Loading from "@/components/loading";

export default function Matching({invite} : any) {

  console.log("invite ", invite);
  return <Loading title="Pending User" subtitle="Please wait ..." />;
}
